# Configuração do Banco de Dados | Database Configuration

DB_DRIVER=sqlite

# Arquivo do SQLite | Archive to SQLite
DB_PATH=database/app.db

# Configurações para MySQL (se necessário) | MySQL settings (if needed)
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=cobol_api
DB_USERNAME=root
DB_PASSWORD=
 
# Pool de conexões | Connection pool
DB_POOL=5
DB_TIMEOUT=30
